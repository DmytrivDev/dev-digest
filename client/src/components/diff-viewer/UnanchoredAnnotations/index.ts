/* UnanchoredAnnotations — the block at the end of a file card for annotations
   whose line is not in the patch. Public surface: the component itself. */
export { UnanchoredAnnotations } from "./UnanchoredAnnotations";
