/* StatsTab — the skill editor's usage statistics: which agents carry the skill,
   how many runs and findings its runs produced, and how those were triaged.
   Public surface: the component itself. */
export { StatsTab } from "./StatsTab";
