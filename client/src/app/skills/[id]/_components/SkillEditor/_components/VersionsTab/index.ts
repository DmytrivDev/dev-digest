/* VersionsTab — the skill editor's body-snapshot history: diff a version
   against the current body and restore it forward as a new version.
   Public surface: the component itself. */
export { VersionsTab } from "./VersionsTab";
