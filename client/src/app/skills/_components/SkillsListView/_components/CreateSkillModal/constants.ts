/** Constants for CreateSkillModal. The selectable types live in `@/lib/skills`
    — the editor's Config tab offers the same list. */

/** Modal width (px) — matches CreateAgentModal. */
export const MODAL_WIDTH = 620;
