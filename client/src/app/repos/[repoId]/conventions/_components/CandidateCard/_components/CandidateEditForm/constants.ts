/** Rows for the rule editor: a convention is one or two sentences, not a body. */
export const EDIT_ROWS = 3;

/** Category menu width — fits "error handling" without stretching the row. */
export const CATEGORY_MENU_WIDTH = 190;
