/* CandidateCard — one convention candidate: rule, evidence, confidence and its
   Accept / Reject / inline Edit. Public surface: the component itself. */
export { CandidateCard } from "./CandidateCard";
