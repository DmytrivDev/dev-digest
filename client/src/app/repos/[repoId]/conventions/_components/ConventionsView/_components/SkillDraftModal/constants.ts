/** Mock's artboard width for this modal — wide enough to read the markdown body. */
export const MODAL_WIDTH = 760;

/** The body is the whole skill, so it gets the room a body needs. */
export const BODY_ROWS = 14;
