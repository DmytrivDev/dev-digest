import type { CSSProperties } from "react";

/** Co-located styles for TriageFilter. */
export const s = {
  row: { display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" } satisfies CSSProperties,
} as const;
