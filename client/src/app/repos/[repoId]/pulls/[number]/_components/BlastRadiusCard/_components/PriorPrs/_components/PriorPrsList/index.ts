/* PriorPrsList — the Prior PRs panel's list, mounted only while the panel is
   open (so the query fires lazily). Public surface: the component itself. */
export { PriorPrsList } from "./PriorPrsList";
